name: QA
on:
  workflow_call:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  lint:
    name: Format Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: ./.github/actions/setup
      - name: Check formatting
        run: forge fmt --check

  build:
    name: Build & Test
    needs: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1
        with:
          version: 1.5.1
      - name: Install dependencies
        run: forge install
      - name: Build
        run: forge build
      - name: Run tests
        run: forge test -vvv
        env:
          BASE_RPC_URL: ${{ secrets.RPC_URL }}
